# Production: full URL of the Render (or other) backend API, including /api/v1
# Example: https://testap-api.onrender.com/api/v1
VITE_API_BASE_URL=

# Local dev: leave empty — Vite proxy forwards /api to localhost:8000
